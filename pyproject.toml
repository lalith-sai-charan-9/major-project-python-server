[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "music-genre-classifier"
version = "1.0.0"
description = "Music Genre Classification API"
requires-python = ">=3.10"
